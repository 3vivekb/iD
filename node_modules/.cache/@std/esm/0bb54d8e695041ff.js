"main";let addSetEntry;_695‍.w('./_addSetEntry.js',[["default",function(v){addSetEntry=v}]]);let arrayReduce;_695‍.w('./_arrayReduce.js',[["default",function(v){arrayReduce=v}]]);let setToArray;_695‍.w('./_setToArray.js',[["default",function(v){setToArray=v}]]);



/** Used to compose bitmasks for cloning. */
var CLONE_DEEP_FLAG = 1;

/**
 * Creates a clone of `set`.
 *
 * @private
 * @param {Object} set The set to clone.
 * @param {Function} cloneFunc The function to clone values.
 * @param {boolean} [isDeep] Specify a deep clone.
 * @returns {Object} Returns the cloned set.
 */
function cloneSet(set, isDeep, cloneFunc) {
  var array = isDeep ? cloneFunc(setToArray(set), CLONE_DEEP_FLAG) : setToArray(set);
  return arrayReduce(array, addSetEntry, new set.constructor);
}

_695‍.d(cloneSet);
