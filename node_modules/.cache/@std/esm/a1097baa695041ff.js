"main";let baseToPairs;_695‍.w('./_baseToPairs.js',[["default",function(v){baseToPairs=v}]]);let getTag;_695‍.w('./_getTag.js',[["default",function(v){getTag=v}]]);let mapToArray;_695‍.w('./_mapToArray.js',[["default",function(v){mapToArray=v}]]);let setToPairs;_695‍.w('./_setToPairs.js',[["default",function(v){setToPairs=v}]]);




/** `Object#toString` result references. */
var mapTag = '[object Map]',
    setTag = '[object Set]';

/**
 * Creates a `_.toPairs` or `_.toPairsIn` function.
 *
 * @private
 * @param {Function} keysFunc The function to get the keys of a given object.
 * @returns {Function} Returns the new pairs function.
 */
function createToPairs(keysFunc) {
  return function(object) {
    var tag = getTag(object);
    if (tag == mapTag) {
      return mapToArray(object);
    }
    if (tag == setTag) {
      return setToPairs(object);
    }
    return baseToPairs(object, keysFunc(object));
  };
}

_695‍.d(createToPairs);
